# Python build / packaging artifacts
*.egg-info/
build/

# Byte-compiled / cache
__pycache__/
*.py[cod]
.cache/

# uv / virtual env (uv.lock is tracked for reproducible installs)
.venv/

# Editors / tooling
.cursor/
.claude/

# Datasets, checkpoints, and run outputs (user-generated, not shipped)
data/
checkpoints/
outputs/
runs/
results/
logs/
*.log
wandb/
figs/
figures/
tmp/

# Cluster launch scripts (kept local; reproduce via config/ + torchrun — see README)
train_fprm_*.sh

# Test-time-scaling eval tools (research-only; not part of the published recipe)
eval_sweep.sh
singlez_eval_sweep.py
